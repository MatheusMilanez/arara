# URL da API do backend. NEXT_PUBLIC_ pra funcionar tanto em Server quanto em
# Client Components (a maioria dos fetches é server-side — ver TECHNICAL_SPEC
# seção 5 — mas o valor precisa estar disponível nos dois casos).
NEXT_PUBLIC_API_URL=http://localhost:3000
